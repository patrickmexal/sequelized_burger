<img src="/public/assets/img/burger.png">
<h1>Eat-Da-Burger!</h1>
<h2>Enter a burger that you would like to eat!</h2>
<ul>
    {{#each burger}} {{#unless devoured}} {{> burger/burger-block devoured=true}} {{/unless}} {{/each}}
</ul>
<h2>Burgers to Devour!</h2>
<ul>
    {{#each burger}} {{#if devoured}} {{> burger/burger-block devoured=false}} {{/if}} {{/each}}
</ul>
<h2>Add a burger to devour!</h2>
<form class="create-form">
    <div class="form-group">
        <label for="burgName">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>
    <button type="submit">Add Burger</button>
</form>